task createDistZip(type: Zip) {
    archiveFileName = 'trainSchedule.zip'
    destinationDirectory = file("${buildDir}/dist")
    from 'src'
}

// Ensure the 'dist' directory is created before the 'createDistZip' task runs
createDistZip.dependsOn(clean, mkdir("${buildDir}/dist"))

